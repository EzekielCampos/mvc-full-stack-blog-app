
 {{!-- Each post will be rendered in the card --}}
 <div class="card mb-5">
{{!-- The user will be redirected to modify the post that was selecte4d --}}
    <a href="/modify/{{id}}" style=" text-decoration: none;">
     <div class="card-header d-flex justify-content-between align-items-center text-white bg-dark" style="height: 50px;">
        <h5 class="mb-0">{{title}} </h5>
        <small> {{format_date date_created}}</small>
      </div>
    </a>

 </div>
